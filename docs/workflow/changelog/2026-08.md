# Changelog 2026-08

## Purpose
Append-only record of completed implementation outcomes for August 2026.

## Entries
- 2026-08-25 | T89 ([docs/workflow/done/T89-strict-openai-system-message-compat.md](../done/T89-strict-openai-system-message-compat.md)) | Fixed strict OpenAI-compatible system-message rejection for FreeToken by enforcing strict OpenAI message-profile behavior on runtime openai-prefixed models (disable inline system prompts and multiple leading system messages) and extracting compatibility logic to a focused module to keep orchestrator boundaries clean; files: `lllars_core/agent_builder.py`, `lllars_core/openai_compat.py`, `tests/test_agent_builder.py`, `docs/workflow/tasks/T89-strict-openai-system-message-compat.md`; validation: PASS `./venv/Scripts/python.exe -m unittest discover -s tests -p "test_agent_builder.py"`, PASS `./venv/Scripts/python.exe -m unittest discover -s tests -p "test_config.py"`, PASS `./venv/Scripts/python.exe -m unittest discover -s tests -p "test_refactor_boundaries.py"`, PASS `./venv/Scripts/python.exe -m unittest discover ./tests/` (141 tests); residual risk: profile enforcement is intentionally global for runtime openai-prefixed models in this repository and may constrain providers that require permissive inline-system behavior.
- 2026-08-13 | T87 ([docs/workflow/done/T87-tighten-stopcheck-and-boundary-enforcement.md](../done/T87-tighten-stopcheck-and-boundary-enforcement.md)) | Tightened rule enforcement with minimal scope: added explicit pre-action stop-check verdict emission gate, added boundary anti-ping-pong no-relocate-twice rule, and expanded forcing-line trigger to fire at first boundary conflict or policy-interpretation branch; files: `AGENTS.md`, `.github/agents/friday.agent.md`, `docs/workflow/done/T87-tighten-stopcheck-and-boundary-enforcement.md`; validation: PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_markdown_boundaries.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_done_task_evaluation_boundaries.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover .\tests\` (139 tests); residual risk: no automated enforcement yet for stop-check verdict presence in all future runs.
- 2026-08-13 | T86 ([docs/workflow/done/T86-fix-runtime-external-profile-override-resolution.md](../done/T86-fix-runtime-external-profile-override-resolution.md)) | Fixed runtime command-profile override resolution for externally configured profile maps by persisting full profile registry state in loaded harness config and using that registry during job-level command_profile selection/allowlist resolution; modularized runtime input assembly into dedicated builder module to satisfy refactor boundaries without in-file compression; files: `lllars_core/config/models.py`, `lllars_core/config/loader_steps.py`, `lllars_core/config/runtime_inputs_builder.py`, `lllars_core/config/harness_builder.py`, `lllars_core/config/loader.py`, `lllars_core/runtime/settings.py`, `tests/test_runtime_runner_overrides.py`, `docs/workflow/done/T86-fix-runtime-external-profile-override-resolution.md`; validation: PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_refactor_boundaries.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_runtime_runner_overrides.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_done_task_evaluation_boundaries.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover -s tests -p "test_markdown_boundaries.py"`, PASS `.\venv\Scripts\python.exe -m unittest discover .\tests\` (139 tests); residual risk: none identified.

## Notes
- New entries should follow the repository-local bookkeeping skill and workflow playbook.
